export type Role = "CUSTOMER" | "ADMIN";
export type User = { id:string; name:string; email:string; phone?:string; passwordHash:string; role:Role; theme:"light"|"dark"; createdAt:string; avatarUrl?:string; oauthProvider?:"google"|"supabase"; oauthSubject?:string };
export type TokenTiming = { id:string; text:string; position:number; startMs:number; endMs:number; confidence:number };
export type Sentence = { id:string; position:number; text:string; summary?:string; tokens:TokenTiming[] };
export type ChapterIllustration = { id?:string; src:string; alt:string; afterSentenceId?:string; position?:number; storagePath?:string };
export type ChapterSection = { id:string; title:string; sentenceId:string; position:number };
export type Chapter = { id:string; bookId:string; title:string; position:number; durationMs:number; isSample:boolean; sentences:Sentence[]; illustrations?:ChapterIllustration[]; sentenceCount?:number; contentFile?:string; sections?:ChapterSection[]; volumeNumber?:number; volumePosition?:number };
export type Book = { id:string; slug:string; title:string; author:string; synopsis:string; priceMinor:number; currency:string; genre:string; tags:string[]; coverTheme:string; coverUrl?:string; status:"PUBLISHED"|"DRAFT"; rating:number; chapters:Chapter[]; contentUnitLabel?:string; contentUnitLabelPlural?:string; documentFile?:string; pageCount?:number };
export type OrderStatus = "PENDING" | "COMPLETED" | "CANCELLED" | "REFUNDED";
export type Order = { id:string; userId:string; bookIds:string[]; totalMinor:number; currency:string; status:OrderStatus; createdAt:string; externalId?:string };
export type Progress = { userId:string; bookId:string; chapterId:string; sentenceId?:string; wordId?:string; positionMs:number; percentage:number; updatedAt:string };
export type Bookmark = { id:string; userId:string; bookId:string; chapterId:string; sentenceId:string; createdAt:string };
export type Review = { id:string; userId:string; bookId:string; rating:number; body:string; spoiler:boolean; provider?:"google"|"email"; createdAt:string; updatedAt:string };
export type ChapterComment = { id:string; userId:string; chapterId:string; rating:number; body:string; spoiler:boolean; parentId?:string; provider?:"google"|"email"; createdAt:string; updatedAt:string };
export type ReadingListItem = { userId:string; bookId:string; createdAt:string };
export type ContentReport = { id:string; userId:string; bookId:string; chapterId:string; sentenceId?:string; message:string; status:"OPEN"|"RESOLVED"; createdAt:string; updatedAt:string };
export type Store = { users:User[]; books:Book[]; orders:Order[]; entitlements:{userId:string;bookId:string}[]; progress:Progress[]; bookmarks:Bookmark[]; readingList:ReadingListItem[]; reports:ContentReport[]; reviews:Review[]; chapterComments:ChapterComment[]; refreshTokens:{userId:string;hash:string;expiresAt:string}[]; auditLogs:{id:string;userId:string;action:string;createdAt:string}[] };
